<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
    <mapper namespace="Member">
    <!-- DB에 회원가입정보 저장 -->
    <insert id="insert">
		insert into member values(member_seq.nextval,#{id}, #{pw},#{name},#{phone},#{email},#{zipcode},#{address1},#{address2},sysdate,#{prefer_location},0,default,default )
	</insert>
	<!-- id중복확인 -->
	<select id="idCheck" resultType="int">
		select count(id) from member where id = #{id}
	</select>
	
    </mapper>